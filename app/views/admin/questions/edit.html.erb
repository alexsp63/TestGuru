<h1><%= question_header(@question, @question.test) %></h1>

<%= render 'form' %>

<p>
  <%= link_to 'Back to all questions', admin_test_path(@question.test) %> |
  <%= link_to 'Delete', admin_test_path(@question.test), method: :delete, data: { confirm: 'Are you sure?' } %> 
</p>
