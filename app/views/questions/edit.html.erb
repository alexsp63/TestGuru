

<%= render 'form' %>

<p>
  <%= link_to 'Back to all questions', test_path(@question.test) %> |
  <%= link_to 'Delete', test_path(@question.test), method: :delete, data: { confirm: 'Are you sure?' } %> 
</p>